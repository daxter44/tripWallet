import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'mm.tripWallet.tripWallet',
  appName: 'trips-wallet',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
